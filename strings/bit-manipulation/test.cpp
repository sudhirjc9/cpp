#include<iostream>
using namespace std;
int main(){

    // 5 ways to create a 2D array

    /*
        1. int arr[][Max];
        2. int **ptr  // pointer to array of pointer
            ptr = new int*[num_row];
            for(int i = 0; i<num_row; i++)
            {
                ptr[i] = new int[num_col];
            }

        3. int *ptr[row_size];
            for(int i =0; i < col_size; i++)
                ptr[i] = new int[]; 
    
        4. vector<int>vec[]           // array of vector
        5. vector<vector<int>> vec;     //vector of vector
    */
 
    return 0;
}